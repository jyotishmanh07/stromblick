# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/

# Data — raw SMARD payloads stay out; reproducible via scripts/ingest_smard_api.py
data/raw/
data/clean/*.csv

# keep directory structure, provenance, and the demand snapshot under version control
# (the snapshot is committed so the deployed dashboard serves real data; CC BY 4.0,
#  Bundesnetzagentur | SMARD.de — see data/clean/metadata.json)
!data/clean/.gitkeep
!data/clean/metadata.json
!data/clean/demand_hourly.csv

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local planning / instruction docs — not shipped with the project
/CLAUDE.md
/plan.md
